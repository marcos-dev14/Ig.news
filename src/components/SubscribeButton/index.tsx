import React from 'react';

import { Container } from './styles';

export function SubscribeButton() {
  return (
    <Container>
      <button type="button">
        Subscribe now
      </button>
    </Container>
  );
}